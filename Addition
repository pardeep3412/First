#include <stdio.h>

int main()
{
  int a, b, c; //Defining the variable
  printf("Type two number: "); //Printf function.
  scanf("%d%d", &a, &b); //Takes input from the user.
  c = a + b;
  printf("Sum of %d and %d is %d", a, b, c); //print function
  //result is shwoing on the nineth line
}
